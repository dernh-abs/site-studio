---
name: site-studio-builder
description: "This skill should be used when adding an in-browser visual editor (Studio) on top of an existing website — especially a clone of another site — so non-technical edits (text, images, section data) flow back into the public pages without altering the original layout or copy. Trigger phrases include: add a studio or visual editor to my site, make the site editable, build a /studio like X, clone this site and add an editing mode, attach a Puck editor or page builder to my Next.js app, let me edit the homepage copy or images from a dashboard. Covers the content layer (UCD plus .content/), a translation-merge adapter that guarantees zero-drift fidelity to the original site, a client bootstrap that reconnects Studio edits to the public pages, a Puck-based /studio editor, an optional natural-language command bar, and an env-gated floating entry button."
---

# Site Studio Builder

## Overview

Attach an **additive, non-destructive visual editing layer** to an existing
website. The public site keeps rendering exactly as before; a `/studio` route
lets editors change text / images / section data, and those edits persist and
appear on the public pages after reload. The pattern was proven on a
`medkungfu.com` clone where public pages stayed byte-for-byte faithful to the
original while a studio editing mode was layered on top.

**Portability.** This `SKILL.md` format is also directly usable by **Claude Code**
(identical frontmatter/layout — drop the folder into `~/.claude/skills/`). For
**Codex**, **Trae**, and **ChatGPT**, use the ready-made adapter files in the
repo's `integrations/` folder (same workflow, tool-appropriate wrapper).

## When to use

- Adding an editable mode to a static or server-rendered site (Next.js App
  Router assumed; the pattern generalizes to any React SPA/SSR).
- Cloning an existing site and wanting an admin / edit overlay without forking
  its code.
- Building a Puck-based page editor with a content backend.
- Adding a natural-language command bar ("edit the hero title to X") that maps
  to content patches.

## Hard constraint — clone fidelity

The public pages **MUST remain byte-for-byte faithful** to the original site.
Studio is the **only** addition. Never overwrite clone copy/layout with
editor-authored defaults. The merge adapter enforces this: the original
dictionaries are the **base**; Studio edits only **override** specific keys.

## Architecture

Six cooperating pieces (see `references/architecture.md` for the file map and
verified code patterns):

1. **Content layer** — `.content/` holds a Unified Content Document (UCD):
   `translations.json` (en/zh/ru), `pages/{slug}.json` (section data),
   `navigation.json`, `meta.json`. Generated by `scripts/seed-ucd.ts`
   (**first-run only**).
2. **LocalContentStore** — atomic writes of patch ops into `.content/*`; the
   `document` API reassembles the split files into one UCD.
3. **ContentRuntime** — a module singleton holding the in-memory UCD.
   `translate()` / `getSectionData()` are used by components and fall back to
   the faithful module when `doc === null`.
4. **compat-adapter** — `compatTranslate` **MERGES** the UCD over the hardcoded
   baseline (module = base, UCD = override). A partial UCD can never break
   rendering.
5. **ContentBootstrap** — client component mounted on public pages (skips
   `/studio`) that fetches `/api/studio/document` and `commitDocument()`s it,
   so edits flow to the public site.
6. **Studio UI** — `/studio` (client, Puck) + `NLCommandBar` / `IntentPreview`
   + `StudioFab` (env-gated floating button).

## Workflow

**Step 0 — Prereqs.** Next.js (App Router) + React. **Node ≥ 24** (the managed
`node` v22 is too old for Next 16 — use `C:\Program Files\nodejs` or `nvm`).
Decide the "faithful baseline": the original copy/dictionaries the public site
renders.

**Step 1 — Extract a faithful baseline into modules.** Put site copy in
`src/lib/i18n/translations.ts` (en+zh) and `ru.ts` (ru), plus `seed-data.ts`
for structured section data (hero image, ctaLinks, statKeys…). Components read
via `t()` and `useSectionData(page, id, fallback)` where `fallback` is the seed
constant (prevents hydration mismatch because `doc` is `null` on the server).

**Step 2 — Build the content layer.** Create `scripts/seed-ucd.ts` that reads
those modules and writes `.content/translations.json`, `pages/*.json`,
`navigation.json`, `meta.json`. **First-run only**: if `translations.json`
already exists, skip regeneration so Studio edits survive restarts/builds.
Wire it via `predev`/`prebuild` npm hooks (`"seed": "tsx scripts/seed-ucd.ts"`).
Add `.content/`, `versions/` to `.gitignore`.

**Step 3 — Runtime + compat adapter.** Add `content-runtime.ts` (singleton)
and `compat-adapter.ts` (`compatTranslate` with merge policy). Components switch
from importing `TRANSLATIONS` directly to `useContentRuntime().translate()` /
`useSectionData()`.

**Step 4 — Reconnect to public pages.** Add `ContentBootstrap.tsx` and mount it
in `layout.tsx` inside the language provider, but `return` early when
`pathname.startsWith("/studio")`.

**Step 5 — Studio editor.** Scaffold `/studio` (client) with Puck: a
`PendingPreview` → `IntentPreview` card showing the parsed intent/operations, an
`NLCommandBar` for natural-language input, and a `StudioFab` floating button
(env-gated: show in dev or when `NEXT_PUBLIC_SHOW_STUDIO_FAB === "true"`).

**Step 6 — NL command API.** Add `/api/agent/command` (dryRun + apply) backed
by a `rule-matcher`; persist via `/api/studio/patch` (RFC-6902 ops) →
`LocalContentStore`. **Carry `answer` / `templates` / `error` / `suggestions`
end-to-end** to the preview so "help" shows the command list and failures show
concrete formats.

**Step 7 — Verify.** Typecheck + lint; all public routes 200; home renders
faithful copy; a patch-API edit persists and **survives a dev restart**; drift
check (merged UCD vs module) shows 0 differences. See `references/architecture.md`
for the drift-check method.

## Critical pitfalls

- **Node version.** The Bash default `node` may be v22 (too old for Next 16).
  Invoke `node`/`npm` from `C:\Program Files\nodejs` (or `nvm use 24`) explicitly.
- **safe-delete hook.** The sandbox blocks `rm -rf` on a large directory and
  `npx playwright install` (trash ops). Clear `.next` via PowerShell
  `Remove-Item`, not `rm -rf`. Do not rely on Playwright for E2E in this env.
- **Seed must be first-run-only.** Otherwise every restart/build wipes Studio
  edits. Guard on `translations.json` existence; tell users to delete `.content/`
  to reset to the faithful baseline.
- **Merge, don't replace.** `compatTranslate` must be `{...module, ...UCD}`;
  replacing with the UCD breaks any key the UCD does not cover.
- **ContentBootstrap must skip `/studio`** or it clobbers the editor's own
  document instance.
- **Hydration.** SSR and first client render must be identical — pass the
  seed-data constant as the `useSectionData` fallback (the UCD is `null` on the
  server).

## Resources

- `references/architecture.md` — file-by-file map, data-flow, and the verified
  code patterns (runtime singleton, merge adapter, bootstrap, seed guard).
- `references/nl-command-bar.md` — the natural-language editing subsystem (API
  contract, rule-matcher, end-to-end carry-through, and the two known UI-bug
  fixes).
